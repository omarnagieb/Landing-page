<!-- The Project Title -->
Landing Page

<!-- changes -->
i have changed the old code by a new one to give all sections their properties dynamically  

<!-- The Project consists of  -->
The project consists of four sections, and every section contains two paragraphs.

the navigation bar contains the section with the number of the section .

the navigation bar added using javascript and styled using css.

it scrolls smoothly using only javascript code without using css.

Responsive with all display sizes.

the navigation bar elements gets you to the certain section you selected from the navigation bar.

only the section that displayed in the screen will be active.

using cursor property to change the styling of the cursor to be effective in all probabilites.
